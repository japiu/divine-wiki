{
  "name": "divine-submit-pr",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that opens a PR on the Divine Skins wiki on behalf of a signed-in contributor. Runs at the edge, rate-limits via KV.",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "wrangler types"
  },
  "dependencies": {
    "@octokit/rest": "^21.1.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.9.3",
    "wrangler": "^3.100.0"
  }
}
